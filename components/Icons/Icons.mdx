---
name: Icons
menu: Components
route: /components/icons
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { IconDemo } from './IconDemo';
import {
  Icon,
  Hamburger,
  Checkmark,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  CloudUpload,
  HeartRegular,
  HeartSolid,
  Image,
  PartyParrot,
  StarRegular,
  StarSolid,
  ThumbsUp,
  Times,
} from './';

# Icon

## SVG

### Size

<Playground>
  <Container>
    <PartyParrot size="small" />
    <PartyParrot size="medium" />
    <PartyParrot size="large" />
  </Container>
</Playground>

### Width & Height

<Playground>
  <Container>
    <PartyParrot width={50} height={50} />
  </Container>
</Playground>

### Color

<Playground>
  <Container>
    <HeartSolid color="red" />
    <HeartSolid color="green" />
    <HeartSolid color="blue" />
  </Container>
</Playground>

### API

<Props of={Icon} />

### Gallery

<Playground>
  <Container>
    <Checkmark />
    <ChevronDown />
    <ChevronLeft />
    <ChevronRight />
    <ChevronUp />
    <CloudUpload />
    <HeartRegular />
    <HeartSolid />
    <Image />
    <PartyParrot />
    <StarRegular />
    <StarSolid />
    <ThumbsUp />
    <Times />
  </Container>
</Playground>

### References

[Font Awesome](https://fontawesome.com)

## Hamburger

<Playground>
  <Container>
    <IconDemo>
      {({ isActive, onClick }) => (
        <Hamburger isActive={isActive} onClick={onClick} size="small" />
      )}
    </IconDemo>
    <IconDemo>
      {({ isActive, onClick }) => (
        <Hamburger isActive={isActive} onClick={onClick} size="medium" />
      )}
    </IconDemo>
    <IconDemo>
      {({ isActive, onClick }) => (
        <Hamburger isActive={isActive} onClick={onClick} size="large" />
      )}
    </IconDemo>
  </Container>
</Playground>

### API

<Props of={Hamburger} />

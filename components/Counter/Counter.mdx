---
name: Counter
menu: Pens
route: /pens/counter
---

import { Props, Playground } from 'docz';
import faker from 'faker';

import { Button } from '../Button';

import { Counter } from './';
import { CounterDemo } from './CounterDemo';

# Counter

Dependencies: [react-countup](https://github.com/glennreyes/react-countup) and [react-visibility-sensor](https://github.com/joshwnj/react-visibility-sensor)

## Demo

### Reset and Re-Animate on Visibility Change

Scroll down and scroll up again to test it

<Playground>
  <Counter isResetEnabled start={0} end={10000} />
</Playground>

### Prefix, Separator & Suffix

<Playground>
  <Counter start={0} end={10000} prefix="$ " separator="," suffix=" left" />
</Playground>

### Auto-Update New End

<Playground>
  <CounterDemo>
    {({ end, updateEnd }) => (
      <>
        <Counter start={0} end={end} />
        <Button onClick={updateEnd}>Update End</Button>
      </>
    )}
  </CounterDemo>
</Playground>

## API

<Props of={Counter} />

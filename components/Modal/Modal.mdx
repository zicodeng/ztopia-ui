---
name: Modal
menu: Components
route: /components/modal
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';
import faker from 'faker';

import { Modal } from './';
import { ModalDemo, Trigger } from './ModalDemo.jsx';

# Modal

Dependency: [react-modal](https://github.com/reactjs/react-modal)

## Demo

### Size

<Playground>
  <Container>
    <ModalDemo>
      {({ isOpen, openModal, closeModal }) => (
        <>
          <Trigger onClick={openModal}>Small</Trigger>
          <Modal size="small" isOpen={isOpen} onRequestClose={closeModal}>
            <h1>Hello, World!</h1>
            <p>{faker.lorem.paragraphs()}</p>
          </Modal>
        </>
      )}
    </ModalDemo>
    <ModalDemo>
      {({ isOpen, openModal, closeModal }) => (
        <>
          <Trigger onClick={openModal}>Medium (Default)</Trigger>
          <Modal size="medium" isOpen={isOpen} onRequestClose={closeModal}>
            <h1>Hello, World!</h1>
            <p>{faker.lorem.paragraphs()}</p>
          </Modal>
        </>
      )}
    </ModalDemo>
    <ModalDemo>
      {({ isOpen, openModal, closeModal }) => (
        <>
          <Trigger onClick={openModal}>Large</Trigger>
          <Modal size="large" isOpen={isOpen} onRequestClose={closeModal}>
            <h1>Hello, World!</h1>
            <p>{faker.lorem.paragraphs()}</p>
          </Modal>
        </>
      )}
    </ModalDemo>
  </Container>
</Playground>

### Show Close Icon

<Playground>
  <ModalDemo>
    {({ isOpen, openModal, closeModal }) => (
      <>
        <Trigger onClick={openModal}>Show Close Icon</Trigger>
        <Modal isCloseIconShown isOpen={isOpen} onRequestClose={closeModal}>
          <h1>Hello, World!</h1>
          <p>{faker.lorem.paragraphs()}</p>
        </Modal>
      </>
    )}
  </ModalDemo>
</Playground>

## API

<Props of={Modal} />

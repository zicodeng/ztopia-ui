---
name: Toggle
menu: Components
route: /components/toggle
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { Toggle } from './';
import { ToggleDemo } from './ToggleDemo.jsx';

# Toggle

Dependency: [react-toggle](https://github.com/aaronshaf/react-toggle)

## Demo

### Size

<Playground>
  <Container>
    <ToggleDemo>
      {({ isChecked, onChange }) => (
        <Toggle size="small" isChecked={isChecked} onChange={onChange} />
      )}
    </ToggleDemo>
    <ToggleDemo>
      {({ isChecked, onChange }) => (
        <Toggle size="medium" isChecked={isChecked} onChange={onChange} />
      )}
    </ToggleDemo>
    <ToggleDemo>
      {({ isChecked, onChange }) => (
        <Toggle size="large" isChecked={isChecked} onChange={onChange} />
      )}
    </ToggleDemo>
  </Container>
</Playground>

## Disabled

<Playground>
  <ToggleDemo>
    {({ isChecked, onChange }) => (
      <Toggle isDisabled isChecked={isChecked} onChange={onChange} />
    )}
  </ToggleDemo>
</Playground>

## Icons

<Playground>
  <ToggleDemo>
    {({ isChecked, onChange }) => (
      <Toggle
        isChecked={isChecked}
        onChange={onChange}
        icons={{
          checked: <i className="fas fa-heart" />,
          unchecked: <i className="fas fa-heart-broken" />,
        }}
      />
    )}
  </ToggleDemo>
</Playground>

## API

<Props of={Toggle} />

```ts
interface ToggleIcons {
  checked?: ReactNode;
  unchecked?: ReactNode;
}
```

---
name: Button
menu: Components
route: /components/button
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { BasicLoader, MusicLoader, FadingLoader } from '../Loaders';

import { Button } from './';

# Button

## Demo

### Variant

<Playground>
  <Container>
    <Button variant="rect">Rect (default)</Button>
    <Button variant="circle" style={{ width: 100, height: 100 }}>
      Circle
    </Button>
    <Button variant="pill">Pill</Button>
    <Button variant="text">Text</Button>
    <Button variant="icon">
      <i className="fas fa-heart" />
    </Button>
  </Container>
</Playground>

### Ghost

<Playground>
  <Container>
    <Button isGhost variant="rect">
      Rect (default)
    </Button>
    <Button isGhost variant="circle" style={{ width: 100, height: 100 }}>
      Circle
    </Button>
    <Button isGhost variant="pill">
      Pill
    </Button>
  </Container>
</Playground>

### Disabled

<Playground>
  <Button isDisabled>Disabled</Button>
</Playground>

### Size

<Playground>
  <Container>
    <Button size="small">Small</Button>
    <Button size="medium">Medium (default)</Button>
    <Button size="large">Large</Button>
  </Container>
</Playground>

### Loading

<Playground>
  <Container>
    <Button loader={<BasicLoader size="small" color="white" />}>Loading</Button>
    <Button loader={<MusicLoader size="small" color="white" />}>Loading</Button>
    <Button loader={<FadingLoader size="small" color="white" />}>
      Loading
    </Button>
  </Container>
</Playground>

## API

<Props of={Button} />

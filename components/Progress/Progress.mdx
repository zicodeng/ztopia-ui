---
name: Progress
menu: Components
route: /components/progress
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { Progress } from './';
import { ProgressDemo } from './ProgressDemo';

# Progress

Dependency: [rc-progress](https://github.com/react-component/progress)

## Demo

### Variant

<Playground>
  <Container>
    <ProgressDemo>
      {({ percent }) => <Progress variant="line" percent={percent} />}
    </ProgressDemo>
    <ProgressDemo>
      {({ percent }) => <Progress variant="circle" percent={percent} />}
    </ProgressDemo>
  </Container>
</Playground>

### Stroke & Trail Style

<Playground>
  <Container>
    <ProgressDemo>
      {({ percent }) => (
        <Progress strokeWidth={4} strokeColor="#4b89da" percent={percent} />
      )}
    </ProgressDemo>
    <ProgressDemo>
      {({ percent }) => (
        <Progress
          variant="circle"
          strokeWidth={4}
          trailWidth={4}
          strokeColor="#d94452"
          trailColor="#f76d82"
          percent={percent}
        />
      )}
    </ProgressDemo>
  </Container>
</Playground>

### Gradient

<Playground>
  <ProgressDemo>
    {({ percent }) => <Progress isGradientEnabled percent={percent} />}
  </ProgressDemo>
</Playground>

## API

<Props of={Progress} />

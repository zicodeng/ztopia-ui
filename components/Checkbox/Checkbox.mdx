---
name: Checkbox
menu: Components
route: /components/checkbox
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { Checkbox } from './';
import { CheckboxDemo } from './CheckboxDemo.jsx';

# Checkbox

## Demo

### Label & Id

<Playground>
  <Container>
    <CheckboxDemo>
      {({ isChecked, onChange }) => (
        <Checkbox
          label="Click checkbox"
          isChecked={isChecked}
          onChange={onChange}
        />
      )}
    </CheckboxDemo>
    <CheckboxDemo>
      {({ isChecked, onChange }) => (
        <Checkbox
          label="Click my label or checkbox"
          id="with-id-demo"
          isChecked={isChecked}
          onChange={onChange}
        />
      )}
    </CheckboxDemo>
  </Container>
</Playground>

## API

<Props of={Checkbox} />

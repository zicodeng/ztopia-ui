---
name: Slider
menu: Components
route: /components/slider
---

import { Props, Playground } from 'docz';

import { Slider } from './';
import { BasicSliderDemo, RangeSliderDemo } from './SliderDemo';

# Slider

Dependency: [rc-slider](https://github.com/react-component/slider/)

## Demo

### Min & Max

<Playground>
  <BasicSliderDemo>
    {({ value, onChange }) => (
      <Slider min={10} max={50} value={value} onChange={onChange} />
    )}
  </BasicSliderDemo>
</Playground>

### Step & Marks

<Playground>
  <BasicSliderDemo>
    {({ value, onChange }) => (
      <Slider
        step={25}
        marks={{
          0: <h1>0</h1>,
          25: <h1>25</h1>,
          50: <h1>50</h1>,
          75: <h1>75</h1>,
          100: <h1>100</h1>,
        }}
        value={value}
        onChange={onChange}
      />
    )}
  </BasicSliderDemo>
</Playground>

### Vertical

<Playground>
  <BasicSliderDemo>
    {({ value, onChange }) => (
      <Slider isVertical value={value} onChange={onChange} />
    )}
  </BasicSliderDemo>
</Playground>

### Range

<Playground>
  <RangeSliderDemo>
    {({ value, onChange }) => (
      <Slider variant="range" value={value} onChange={onChange} />
    )}
  </RangeSliderDemo>
</Playground>

## API

<Props of={Slider} />

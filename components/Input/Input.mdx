---
name: Input
menu: Components
route: /components/input
---

import { Props, Playground } from 'docz';
import { Container } from 'docz-theme-ztopia/helpers';

import { Input } from './';
import { InputDemo } from './InputDemo';

# Input

## Demo

### Variant

<Playground>
  <Container>
    <InputDemo>
      {({ value, onChange }) => (
        <Input label="Rect" variant="rect" value={value} onChange={onChange} />
      )}
    </InputDemo>
    <InputDemo>
      {({ value, onChange }) => (
        <Input label="Pill" variant="pill" value={value} onChange={onChange} />
      )}
    </InputDemo>
    <InputDemo>
      {({ value, onChange }) => (
        <Input
          label="Material"
          variant="material"
          value={value}
          onChange={onChange}
        />
      )}
    </InputDemo>
  </Container>
</Playground>

### Error

<Playground>
  <Container>
    <InputDemo>
      {({ value, onChange }) => (
        <Input
          type="email"
          label="Email"
          variant="rect"
          error="Invalid email"
          value={value}
          onChange={onChange}
        />
      )}
    </InputDemo>
    <InputDemo>
      {({ value, onChange }) => (
        <Input
          type="email"
          label="Email"
          variant="pill"
          error="Invalid email"
          value={value}
          onChange={onChange}
        />
      )}
    </InputDemo>
    <InputDemo>
      {({ value, onChange }) => (
        <Input
          type="email"
          label="Email"
          variant="material"
          error="Invalid email"
          value={value}
          onChange={onChange}
        />
      )}
    </InputDemo>
  </Container>
</Playground>

## API

<Props of={Input} />

---
name: Select
menu: Components
route: /components/select
---

import { Props, Playground } from 'docz';

import { Select } from './';
import { options, SelectDemo } from './SelectDemo.jsx';

# Select

Dependency: [react-select](https://github.com/JedWatson/react-select)

## Demo

### Single Selection

<Playground>
  <SelectDemo>
    {({ value, onChange }) => (
      <Select
        label="What is your favorite fruit?"
        value={value}
        options={options}
        onChange={onChange}
      />
    )}
  </SelectDemo>
</Playground>

### Multi Selection

<Playground>
  <SelectDemo>
    {({ value, onChange }) => (
      <Select
        isMulti
        label="What are your favorite fruits?"
        value={value}
        options={options}
        onChange={onChange}
      />
    )}
  </SelectDemo>
</Playground>

### Searchable

<Playground>
  <SelectDemo>
    {({ value, onChange }) => (
      <Select
        isSearchable
        label="What are your favorite fruits?"
        value={value}
        options={options}
        onChange={onChange}
      />
    )}
  </SelectDemo>
</Playground>

### Error

<Playground>
  <SelectDemo>
    {({ value, onChange }) => (
      <Select
        label="What is your favorite fruit?"
        value={value}
        error="Empty value"
        options={options}
        onChange={onChange}
      />
    )}
  </SelectDemo>
</Playground>

## API

<Props of={Select} />
